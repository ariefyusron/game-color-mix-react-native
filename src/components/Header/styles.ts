import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "100%",
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: "#fff",
    elevation: 5,
  },
  title: {
    fontWeight: "bold",
    fontSize: 18,
    marginLeft: 32,
  },
  buttonMenu: {
    padding: 16,
  },
});

export default styles;
