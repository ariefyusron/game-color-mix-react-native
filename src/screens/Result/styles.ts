import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    padding: 32,
  },
  title: {
    color: "#000",
    fontWeight: "bold",
    fontSize: 32,
    marginBottom: 16,
  },
  wrapButton: {
    alignItems: "center",
    marginTop: 32,
  },
});

export default styles;
