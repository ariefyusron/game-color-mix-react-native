import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    padding: 32,
  },
  title: {
    fontWeight: "bold",
    fontSize: 32,
    color: "#000",
    textAlign: "center",
    marginBottom: 16,
  },
  desc: {
    color: "#000",
    lineHeight: 24,
    fontSize: 16,
  },
  wrapButton: {
    alignItems: "center",
    marginTop: 48,
  },
});

export default styles;
